CREATE TABLE IF NOT EXISTS media (id SERIAL PRIMARY KEY, sha1 VARCHAR(255) NOT NULL, "type" VARCHAR(255) NOT NULL, last_accessed_at TIMESTAMP NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL, updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL);
